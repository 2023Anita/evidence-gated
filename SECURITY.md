# 安全策略

## 报告安全问题

请通过 GitHub Security Advisories 的私密报告入口提交安全问题。不要在公开 Issue 中发布可直接利用的绕过步骤、凭据或用户数据。

报告最好包含受影响版本、最小复现、预期与实际行为，以及问题是否能伪造审批、绕过范围检查、替换可信验证器或让失败 CI 被接受。

## 支持范围

当前维护版本：`0.1.x`。

## 重要边界

该插件不能控制拥有管理员权限、治理仓库写权限、审批私钥或 GitHub bypass 的攻击者。公开文档中的本地状态、报告和 `--ci` 标志本身都不是可信授权。

完整威胁模型见 [docs/threat-model.md](docs/threat-model.md)。
