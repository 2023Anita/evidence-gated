# 本地验收记录 · 0.1.0

日期：2026-09-16。宿主：macOS arm64，Python 3.13.5。

运行时核心已经改为只依赖 Python 标准库；`requirements-governance.lock` 只用于开发测试与 Schema 交叉验证。

## 已执行

| 项目 | 结果 |
|---|---|
| 干净项目虚拟环境安装 | `requirements-governance.lock` 全部哈希校验通过 |
| 自动化测试 | 54 项通过：CLI 13、核心 29、Git/GitHub mock 12 |
| 真实示例 | bool 校验缺陷被拒绝；修复后通过 |
| 固定验收测试 | 6 项通过 |
| 项目测试 | 4 项通过 |
| 源插件和分发插件元数据 | plugin-creator 校验通过 |
| Codex Skills | 5 份 frontmatter 校验通过 |
| 上游固定快照 | 39/39 文件 SHA256 与 Git blob SHA 一致 |
| Schema | 合同、审批、报告 Schema 结构检查通过；实际报告通过校验 |
| CI YAML | 两份文件均可解析 |
| graphify | 已按仓库约定重建知识图 |

核心测试包含：过期签名、身份不匹配、缺失签名、伪造状态和成功报告、修改治理代码、未跟踪越界文件、保护路径、跳过/空测试、执行超时、验证期间改动、基线篡改、符号链接、报告覆盖代码、并发锁与状态跳步。

实际演示报告位于开发项目 `reports/demo-result.json`。演示使用临时测试身份并自动销毁私钥；不代表正式人工批准。

## 尚未实测

- Docker 后端：当前机器无 Docker。缺失时拒绝而不是回退，已测试；容器实际运行待环境就绪后验证。
- GitHub Required Check、规则与真实 PR：没有发布或修改平台配置，尚未在线验收。
- 新 Codex 会话加载：已经生成本地 marketplace 和安装包，但未注册到个人配置、未安装。
- Windows、Python 3.12 的实际运行、其他 CPU 架构和 merge queue：未验证。
- 医学/科研审核：本版未实现。

## 子代理贡献与主代理复核

`luna_worker` 提供上游快照、五份 Codex Skill 包装、兼容性文档和 13 项 CLI 黑盒测试。
主代理检查路径、调用规则与信任边界，复算快照哈希，并在独立项目虚拟环境运行完整测试；治理核心、证据验证、CI 模板和主要防绕过测试由主代理实现。

## 交付状态

本地源项目和可安装分发包已生成；没有 Git 初始化、分支、提交、推送、远程仓库创建、网站发布或全局 Codex 配置变更。
平台硬门禁是下一步需要部署并独立验收的状态，不能由本记录推定为已完成。
